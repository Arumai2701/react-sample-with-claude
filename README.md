# Vite + React App

Minimal Vite + React (JavaScript) scaffold.

Setup (Windows PowerShell):

```powershell
cd C:\Learning
npm install
npm run dev
```

- `npm run dev` starts the dev server (default: http://localhost:5173)
- `npm run build` builds production files into `dist/`
- `npm run preview` locally previews the production build
